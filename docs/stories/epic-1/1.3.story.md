# Story 1.3: Convite de Usuários e Permissões

| Campo | Valor |
|-------|-------|
| **Epic** | Epic 1: Fundação, Auth e Multi-Tenancy |
| **Status** | Ready |
| **Prioridade** | P1 |

## User Story

**Como** admin de uma marca,
**Quero** convidar membros da minha equipe por email e definir suas permissões,
**Para que** cada pessoa tenha acesso apenas ao que precisa.

## Acceptance Criteria

- [x] 1. Tela de configurações com seção "Equipe" listando usuários do tenant
- [x] 2. Botão "Convidar" abre modal com campo de email e checkboxes de permissões
- [x] 3. Permissões disponíveis: ver campanhas, editar campanhas, ver influencers, editar influencers, ver financeiro, ver mineração, editar branding, gerenciar equipe
- [x] 4. Convite envia email via Supabase Auth (invite user)
- [x] 5. Usuário convidado ao aceitar é vinculado ao tenant com as permissões selecionadas
- [x] 6. Tabela `user_permissions` com: user_id, tenant_id, permission_key, granted (boolean)
- [x] 7. Sistema verifica permissões antes de renderizar seções e executar ações
- [x] 8. Admin pode editar permissões de usuários existentes
- [x] 9. Admin pode remover usuários do tenant

## Technical Notes

- Tabela `user_permissions` já na migration 00001
- Função `has_permission()` disponível para RLS
- UI: PermissionMatrix component (checkboxes grid)
- Supabase `auth.admin.inviteUserByEmail()`

## File List

- `src/app/(dashboard)/settings/page.tsx` — Settings page with TeamSection
- `src/app/(dashboard)/settings/actions.ts` — Server actions (getTeamMembers, inviteUser, updatePermissions, removeMember)
- `src/app/(dashboard)/settings/team-section.tsx` — Team list with table, invite/edit/remove controls
- `src/app/(dashboard)/settings/invite-modal.tsx` — Invite modal with email + permission checkboxes
- `src/app/(dashboard)/settings/permissions-modal.tsx` — Edit permissions modal
- `src/lib/supabase/admin.ts` — Supabase admin client (service role) for inviteUserByEmail
- `supabase/migrations/00001_create_tenants_and_auth.sql` — user_permissions table, RLS policies (pre-existing)

## Dependencies

- Story 1.2 (autenticação funcional)
